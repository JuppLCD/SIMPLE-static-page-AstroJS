---
export interface Props {
	url: string;
}
const { url } = Astro.props;
---

<section class='banner' style={`background-image: url( ${url} );`}>
	<slot />
</section>

<style>
	.banner {
		background-size: cover;
		background-repeat: no-repeat;
		color: #fff;
	}
</style>
